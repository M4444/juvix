mod Prelude.Handler where

open Prelude
open Circuit
open Circuit.Field

handler printer =
  let print = printLn
  let pure x = toString x
end

sig prog : a -> a -- effect
let prog a =
  print a;
  pure a

sig main : a
let main = prog via printer